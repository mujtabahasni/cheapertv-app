import { Component } from '@angular/core';

@Component({
  selector: 'app-profile-form-submit',
  template: `
  <section>
    <button routerLink="/">Save</button>
  </section>
  `
})
export class ProfileFormSubmitComponent {}
